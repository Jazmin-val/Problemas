3.- Escriba un programa que muestre la suma de los 20 primeros múltiplos de 3.
CÓDIGO EN PYTHON: 

def suma_multiplos(): 
suma = 0 
    print("Primeros 20 múltiplos de 3:") 
for i in range(1, 21): 
        multiplo = i * 3 
        suma += multiplo 
        print(f"#{i}: {multiplo}") 
 
  print("-" * 20) 
    print(f"Suma total: {suma}") 
 
if name == "main": 
 suma_multiplos()

